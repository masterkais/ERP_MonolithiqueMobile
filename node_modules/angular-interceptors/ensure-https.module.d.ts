import { ModuleWithProviders } from '@angular/core';
export declare class EnsureHttpsInterceptorModule {
    /**
     * Change `http://` to `https://` in HTTP request urls.
     */
    static forRoot(): ModuleWithProviders;
}
