import { HttpEvent, HttpHandler, HttpInterceptor } from '@angular/common/http';
import { HttpRequest } from '@angular/common/http';
import { InjectionToken } from '@angular/core';
import { Observable } from 'rxjs';
export declare const PREFIX: InjectionToken<string>;
export declare class PrefixUrlInterceptor implements HttpInterceptor {
    prefix: any;
    constructor(prefix: any);
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
}
export declare const PREFIX_URL_INTERCEPTOR_PROVIDER: {
    provide: InjectionToken<HttpInterceptor[]>;
    useClass: typeof PrefixUrlInterceptor;
    multi: boolean;
};
